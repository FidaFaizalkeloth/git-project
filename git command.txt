git config --global user.name "FidaFaizalkeloth"
git config --global user.email "email"

git init
git add .
git remote add origin "https://github.com/FidaFaizalkeloth/git-project.git"
git commit -m "commit_label"
git push -u origin master

from second push:
git add .
git commit -m "commit label"
git push 

clone:
git clone "url"

branch creation;
git status //check branch
git branch "branch name"

pull request:
git pull "url"


